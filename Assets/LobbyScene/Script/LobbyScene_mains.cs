using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LobbyScene_mains : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        BreakData.ResetData();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
