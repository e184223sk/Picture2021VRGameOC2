using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Lock : MonoBehaviour
{
    /*[SerializeField]
    Weapon _myWeapon;


    public Transform ring, Obj; 

    void Update()
    {
        ring.LookAt(Camera.main.transform);
        Obj.Rotate(Vector3.up * Time.deltaTime * 60, Space.World);
    }

    private void OnTriggerEnter(Collider other)
    {
        WeaponManager._HavingWeapon = _myWeapon;
        this.gameObject.SetActive(false);
    }*/
}
